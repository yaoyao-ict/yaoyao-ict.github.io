---
const { cards } = Astro.props;
---

<div class="card-grid">
  {cards.map((card) => (
    <a class="content-card" href={card.href}>
      <span>{card.eyebrow}</span>
      <strong>{card.title}</strong>
      <p>{card.description}</p>
    </a>
  ))}
</div>
